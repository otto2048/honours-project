#include "ExerciseOne.h"

int ExerciseOne::getCarPriceRange()
{
    //int largest = arr[0];
    int largest = 0;
    //int smallest = arr[0];
    int smallest = 0;

    /*for (int i = 0; i < size; i++)
    {
        if (arr[i] > largest)
        {
            largest = arr[i];
        }

        if (arr[i] < smallest)
        {
            smallest = arr[i];
        }
    }*/

    return smallest - largest;
}